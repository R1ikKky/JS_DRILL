export const TOPICS = {
  promises: 'Promise / async-await',
  strings: 'Строки JS',
  arrays: 'Методы массивов',
  mapset: 'Map и Set',
  object: 'Object.keys / values / entries',
  recursion: 'Рекурсия и стек вызовов',
} as const;

export type TopicKey = keyof typeof TOPICS;

const DIFFICULTY_MAP: Record<string, string> = {
  easy: 'базовый',
  medium: 'средний',
  hard: 'сложный',
  mixed: 'любой',
};

export function buildQuestionPrompt(topic: string, difficulty: string): string {
  const topicLabel = TOPICS[topic as TopicKey] ?? topic;
  const difficultyLabel = DIFFICULTY_MAP[difficulty] ?? difficulty;

  return `Ты — строгий интервьюер по JavaScript. Составь одно интервью-задание на тему "${topicLabel}" уровня сложности "${difficultyLabel}".

Правила:
1. Вопрос должен быть на русском языке.
2. Случайно выбери ОДИН из следующих форматов:
   - "Что выведет этот код?" с хитрым фрагментом кода (частые ловушки: замыкания, hoisting, порядок выполнения промисов, коварный typeof)
   - "Напиши функцию, которая..." с конкретным техническим требованием
   - Концептуальный вопрос, где распространённое заблуждение является ловушкой
3. Если используется код — оберни его ТОЧНО в маркеры [CODE]...[/CODE] без пробелов у скобок.
4. Вопрос должен быть конкретным и сфокусированным — не более 150 слов.
5. Не давай подсказок и не упоминай правильный ответ.
6. Начни сразу с вопроса — без преамбул типа "Вот вопрос:" или "Конечно!".`;
}

export function buildEvaluationPrompt(
  topic: string,
  difficulty: string,
  question: string,
  answer: string,
): string {
  const topicLabel = TOPICS[topic as TopicKey] ?? topic;
  void difficulty;

  return `Ты — строгий но справедливый технический интервьюер для JavaScript-разработчика.

Тебе дан вопрос и ответ кандидата. Оцени ответ.

ПРАВИЛА ОЦЕНКИ:
1. Оценивай СУТЬ и ФАКТИЧЕСКУЮ ПРАВИЛЬНОСТЬ — не форму, не стиль, не терминологию.
2. Кандидат может писать на русском, в свободной форме, смешивать объяснения с кодом — это нормально.
3. Разговорные слова ("микротаска", "промис", "колбэк") считай корректными — не снижай оценку за них.
4. Если вопрос про порядок вывода — ответ верный если правильная последовательность значений присутствует в ответе, даже если рядом есть пояснения или комментарии.
5. Частичный зачёт (ЧАСТИЧНО) — если кандидат понял главное, но упустил важный нюанс.
6. НЕВЕРНО — только если есть фактически ошибочное утверждение или полностью неверная логика.

ФОРМАТ ОТВЕТА (строго):
Первая строка — одно из трёх слов: ВЕРНО / ЧАСТИЧНО / НЕВЕРНО
Со второй строки — объяснение на русском: 2-4 предложения.
Если неверно или частично — покажи правильный ответ или код.

Вопрос: ${question}

Ответ кандидата: ${answer}`;
}
